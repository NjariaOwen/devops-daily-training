-- regular file --
the dash (-) represenr user data files that are human readable like text files, images and 
binries

-rw-r--r--   1 root                 root                  3444 Jul  5  2023 adduser.conf

-- directory --
they are shown by (d) and they organize files and other directories hierarchically

drwxr-xr-x   9 root                 root                  4096 Feb  3 13:42 apt

-- symlink --

they are shown by (l) and they are links to another file maybe in another directory even

lrwxrwxrwx  1 owen owen    17 Apr 12 17:19 symlink.txt -> /tmp/original.txt

-- character device --

they are shown by (c) which represents devices that receive or send data using characters 

-- block device --

they are show by (b) wgich represents devices that receive and send data as blocks 

-----

/bin/bash ELF 64-bit LSB pie executable --its an executable
/etc/hosts is an ASCII text
/usr/bin/python3 is a symbolic link to python3.12


